{
  "name": "speed-reader-tests",
  "private": true,
  "type": "module",
  "description": "Test runner for SpeedReader extension JavaScript (dev only, not bundled)",
  "scripts": {
    "test": "bun test tests/js/*.test.js",
    "test:regression": "bun test tests/regression/*.test.js",
    "test:word-processor": "bun test tests/js/word-processor.test.js",
    "test:focus-point": "bun test tests/js/focus-point.test.js",
    "version:bump": "bun run scripts/bump-version.ts"
  },
  "devDependencies": {
    "eslint": "^9.39.4",
    "globals": "^15.15.0"
  }
}
